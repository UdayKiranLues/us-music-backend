# Environment
NODE_ENV=development

# Server
PORT=5000
API_VERSION=v1

# Database
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/us-music?retryWrites=true&w=majority

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your-refresh-token-secret-change-this
JWT_REFRESH_EXPIRES_IN=30d

# CORS
FRONTEND_URL=http://localhost:5173

# AWS S3
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=eu-north-1
AWS_S3_BUCKET=us-music

# AWS CloudFront CDN (Recommended - fixes CORS and DNS issues)
# PUBLIC ACCESS (minimal setup - just need domain):
CLOUDFRONT_DOMAIN=d1234567890.cloudfront.net

# SIGNED URLS (optional - production security with time-limited access):
# Uncomment these lines if you want signed CloudFront URLs
# CLOUDFRONT_KEY_PAIR_ID=APKAXXXXXXXXXXXXX
# CLOUDFRONT_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\nYourPrivateKeyHere\n-----END RSA PRIVATE KEY-----"

# Note: If only CLOUDFRONT_DOMAIN is set, system uses public CloudFront URLs (good for music streaming)
# If all 3 are set, system uses signed CloudFront URLs (more secure, time-limited)
# If none are set, system falls back to S3 presigned URLs (slower, potential CORS issues)
#
# Setup guide: See backend/config/CLOUDFRONT-SETUP.md
# Quick start: See backend/config/CLOUDFRONT-QUICKSTART.txt

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Pagination
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100
